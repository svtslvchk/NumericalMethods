# TODO: Лабораторная работа — Метод вращений Якоби

- [CLOSED] **Step 1: Парсер файла + Вспомогательные функции (`io_utils.py`)**
  - [x] Реализовать / переиспользовать функцию `read_file(filename)` для чтения квадратной матрицы $A$ из файла.
  - [x] Реализовать функцию `print_matrix(matrix, name)` для форматированного вывода 2D-матриц.
  - [x] Реализовать функцию `print_vector(vec, name)` для вывода собственных значений.
  - [x] Написать функции перемножения матриц `matmul(A, B)` и матрицы на вектор `mat_vec_mul(A, x)`.

- [CLOSED] **Step 2: Математический и вычислительный модуль (`jacobi_solver.py`)**
  - [x] Реализовать функцию `is_symmetric(A)` для проверки симметричности матрицы ($A = A^T$).
  - [x] Написать функцию `find_max_off_diag(A)` для поиска наиболшего по модулю элемента $a_{ij}$ ($i \neq j$).
  - [x] Написать функцию `off_diag_norm(A)` для вычисления критерия останова $\sqrt{\sum_{i \neq j} a_{ij}^2}$.
  - [x] Реализовать основной метод `jacobi_rotations(A, eps, max_iter=1000)`:
    - [ ] Инициализировать матрицу собственных векторов $V = I$.
    - [ ] Вычислять угол поворота $\phi$ (значения $\cos\phi$ и $\sin\phi$).
    - [ ] Выполнять локальное ортогональное вращение (пересчет элементов $A$).
    - [ ] Накапливать произведение матриц вращения $V = V \cdot U_k$.
    - [ ] Проверять критерий останова по точности $\varepsilon$.
    - [ ] Возвращать собственное значение $\Lambda = \text{diag}(A)$, матрицу $V$ и число итераций.

- [CLOSED] **Step 3: Вывод результатов и Проверки (Пункты 1–4)**
  - [x] **Пункт 1:** Вывести заданную точность вычислений $\varepsilon$.
  - [x] **Пункт 2:** Вывести вектор найденных собственных значений $\Lambda$.
  - [x] **Пункт 3:** Вывести матрицу собственных векторов $V$ и количество выполненных итераций.
  - [x] **Пункт 4:** Проверка $A \cdot V = V \cdot \Lambda$ (проверка невязки $\|A \cdot V - V \cdot \Lambda\|$).
  - [x] **Эталонная проверка:** Сравнительная валидация собственных значений и векторов через `numpy.linalg.eigh(A)`.