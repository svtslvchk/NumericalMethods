# TODO: Лабораторная работа — LU-разложение (LUP)

- [CLOSED] **Step 1: Парсер файла + Вспомогательные функции**
  - [x] Реализовать функцию `read_file(filename)` для чтения $A$ и $b$ из `input.txt` (разбор уравнений вида `2x1 + x2 = 4`).
  - [x] Реализовать функцию `print_matrix(matrix, name)` для красивого вывода матриц с округлением `float`.
  - [x] Написать функцию `matmul(A, B)` для перемножения двух квадратных матриц $N \times N$.
  - [x] Написать функцию `mat_vec_mul(A, x)` для умножения матрицы на вектор (нужно для проверки $A \cdot x$).

- [ ] **Step 2: Алгоритм LUP-разложения**
  - [x] Реализовать функцию `lu_decompose(A)`, которая возвращает $L$, $U$, матрицу/вектор перестановок $P$ и количество перестановок `num_swaps`.
  - [x] Учесть частичный выбор опорного элемента по столбцам.
  - [ ] Добавить обработку вырожденных матриц (проверка деления на 0).

- [CLOSED] **Step 3: Решение СЛАУ ($A \cdot x = b$)**
  - [x] Реализовать прямой ход (Forward Substitution): решение $L \cdot z = P \cdot b$.
  - [x] Реализовать обратный ход (Backward Substitution): решение $U \cdot x = z$.
  - [x] Объединить всё в функцию `lu_solve(L, U, P, b)`.

- [CLOSED] **Step 4: Определитель и Обратная матрица**
  - [x] Написать функцию `lu_det(U, num_swaps)` для вычисления $\det(A) = (-1)^S \cdot \prod u_{ii}$.
  - [x] Написать функцию `lu_inverse(L, U, P)` путем решения $N$ систем $A \cdot x^{(i)} = e^{(i)}$.

- [ ] **Step 5: Вывод результатов и Проверки (Пункты 1–7)**
  - [x] **Пункт 1:** Вывести матрицы $L$ и $U$.
  - [x] **Пункт 2:** Вывести результат умножения $L \cdot U$.
  - [x] **Пункт 3:** Вывести решение системы $x$ ($A \cdot x = b$).
  - [x] **Пункт 4:** Вывести обратную матрицу $A^{-1}$.
  - [x] **Пункт 5:** Вывести определитель $\det(A)$.
  - [x] **Пункт 6:** Проверка $A \cdot A^{-1} = E$ (должна получиться единичная матрица).
  - [x] **Пункт 7:** Проверка $L \cdot U = P \cdot A$.
  - [ ] **Эталонная проверка:** Сравнить результаты нашей реализации с помощью `numpy.linalg`.