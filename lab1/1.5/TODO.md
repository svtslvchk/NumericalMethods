# TODO: Лабораторная работа 1.5 — QR-алгоритм (Метод Хаусхолдера)

## 1. Подготовка и ввод-вывод (`io_utils.py`)
- [x] Чтение исходной матрицы A из файла `input.txt`
- [ ] Функция красивого вывода векторов, матриц и комплексных чисел

## 2. Вспомогательные матричные операции (`matrix_operations.py`)
- [x] Перемножение матриц `matmul(A, B)`
- [x] Транспонирование `transpose(A)`
- [x] Вычисление евклидовой нормы вектора `vector_norm(x)`
- [x] Внешнее произведение векторов `outer_product(v1, v2)` (для $v v^T$)
- [x] Вычисление нормы поддиагональных элементов (для проверки сходимости)

## 3. QR-разложение Хаусхолдера (`qr_solver.py`)
- [ ] Функция `householder_reflection(A)`:
  - Построение векторов $v$ и матриц $H_k$ для каждого столбца
  - Получение верхнетреугольной матрицы $R$
  - Формирование ортогональной матрицы $Q = H_1 H_2 \dots H_{n-1}$
- [x] Функция `extract_eigenvalues(A)`:
  - Извлечение вещественных корней $\lambda_i = A[i][i]$
  - Поиск блоков $2 \times 2$ на диагонали и вычисление комплексно-сопряженных пар $\alpha \pm i\beta$ через дискриминант
- [x] Функция `qr_algorithm(A, eps, max_iters)`:
  - Итерационный процесс $A_{k+1} = R_k \cdot Q_k$

## 4. Запуск и валидация (`main.py`)
- [x] Вывод исходной матрицы $A$
- [x] QR-разложение на шаге 1: вывод матриц $Q$ и $R$
- [x] Проверка $Q \cdot R = A$ (вычисление невязки)
- [x] Поиск собственных значений QR-алгоритмом
- [x] Тестирование на собственном примере с комплексно-сопряженными корнями
- [x] Валидация результатов через `numpy.linalg.eig`