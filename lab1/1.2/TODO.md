# TODO: Лабораторная работа 1.2 — Метод прогонки

## 1. Модуль Ввода-Вывода (`io_utils.py`)
- [x] Переиспользовать/адаптировать функцию парсинга уравнений `parse_left_part` из 1.1.
- [x] Реализовать `read_file(filename)`:
  - Считывать файл построчно.
  - Раскладывать коэффициенты строго в 4 одномерных массива: `a`, `b`, `c`, `d`.
  - Валидировать, что матрица строго 3-диагональная (нет элементов кроме $x_{i-1}, x_i, x_{i+1}$).
  - *Запрещено создавать плотную 2D матрицу N x N!*
- [x] Реализовать красивую печать прогоночных коэффициентов $P_i, Q_i$ и вектора решений $x$.

## 2. Вычислительный модуль (`tridiagonal_solver.py`)
- [x] Реализовать функцию `check_strict_diagonal_dominance(a, b, c)`:
  - Проверка условий корректности/устойчивости прогонки.
- [x] Реализовать функцию `solve_tridiagonal(a, b, c, d)`:
  - **Прямой ход:**
    - Посчитать $P_1, Q_1$.
    - В цикле от $i=2$ до $n-1$ посчитать $P_i, Q_i$.
    - Посчитать $Q_n$.
  - **Обратный ход:**
    - Установить $x_n = Q_n$.
    - В цикле от $i=n-1$ до $1$ посчитать $x_i = P_i \cdot x_{i+1} + Q_i$.
  - Вернуть списки `P`, `Q`, `x`.

## 3. Точка входа (`main.py`)
- [x] Считать данные из `input.txt`.
- [x] Вызвать `solve_tridiagonal()`.
- [x] Вывести в консоль:
  - Прогоночные коэффициенты $P$ и $Q$.
  - Итоговое решение системы $x$.
- [] **Проверка NumPy (дистанционная validation):**
  - Восстановить плотную матрицу `A_dense` чисто для вызова `np.linalg.solve()`.
  - Сравнить решения собственного метода и `np.linalg.solve()`.