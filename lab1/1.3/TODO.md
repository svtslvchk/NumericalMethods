# TODO: Лабораторная работа 1.3 — Метод простых итераций и метод Зейделя

## 1. Модуль Ввода-Вывода (`io_utils.py`)
- [x] Переиспользовать функции считывания полных систем $A \cdot x = b$ из ЛР 1.1.
- [ ] Реализовать функцию ввода точности $\varepsilon$ (с клавиатуры или из файла).
- [x] Реализовать форматный вывод результатов:
  - Заданная точность $\varepsilon$.
  - Количество итераций $k_{МПИ}$ и $k_{Зейдель}$.
  - Векторы решений $x_{МПИ}$ и $x_{Зейдель}$.
  - Эталонное решение $x_{NumPy}$.
  - Сравнительный вывод скорости сходимости.

## 2. Модуль сходимости и преобразований (`iter_utils.py`)
- [x] Реализовать функцию проверки диагонального преобладания матрицы $A$.
- [x] Реализовать приведение системы $A \cdot x = b$ к виду $x = \alpha \cdot x + \beta$.
- [x] Реализовать вычисление нормы матрицы $\|\alpha\|_\infty$.
- [x] Реализовать вычисление нормы разности векторов $\|x^{(k+1)} - x^{(k)}\|_\infty$.

## 3. Вычислительный модуль (`iterative_solvers.py`)
- [x] Реализовать `solve_simple_iteration(alpha, beta, eps, max_iter=1000)`:
  - Стартовое приближение $x^{(0)} = \beta$.
  - Итерационный цикл по формуле МПИ.
  - Проверка критерия останова.
  - Возврат пары: `(x, iter_count)`.
- [x] Реализовать `solve_seidel(alpha, beta, eps, max_iter=1000)`:
  - Стартовое приближение $x^{(0)} = \beta$.
  - Итерационный цикл с немедленным обновлением элементов $x_i$.
  - Проверка критерия останова.
  - Возврат пары: `(x, iter_count)`.

## 4. Точка входа и Валидация (`main.py`)
- [x] Считать данные системы из `input.txt` и значение $\varepsilon$.
- [x] Привести матрицу к итерационному виду и проверить условие $\|\alpha\| < 1$.
- [x] Выполнить расчет обеими методами.
- [x] Получить эталонное решение через `np.linalg.solve(A, b)`.
- [x] Сравнить количество итераций и вывести аналитическое резюме по скорости сходимости.